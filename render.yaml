services:
  - type: web
    name: cong-usdt-bot
    env: python
    buildCommand: ""
    startCommand: "python main.py"
    autoDeploy: false
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: CHAT_ID
        sync: false
      - key: TARGET_URL
        sync: false
